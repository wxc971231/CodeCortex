# Forge Benchmark Fixture

Forge is a small training service used only to freeze CodeCortex M1b
evaluation cases.  It saves atomic checkpoints, evaluates a batch, renders a
report, and exposes a deliberately ordinary command-line parser.
